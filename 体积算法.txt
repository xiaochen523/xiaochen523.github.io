#include<stdio.h>
#define height 10

int calculate(width,long)
int main()
{
   printf("输入宽度\n");
   scanf("%d",&width);

   printf("输入长度\n”);
   scanf("%d",&long);

   result=calculate(long,width);
   printf("长方体的体积是：");
   printf("%d\n",result);
   return 0;
}

int calculate(int width,int long)
{
    int result=long*width*height;
    result;
}
